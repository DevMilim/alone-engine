[package]
name = "net"
version = "0.1.0"
edition = "2024"

[dependencies]
bincode = "2"
bytes = "1.11.1"
crossbeam-channel = "0.5.15"
serde = { version = "1.0.228", features = ["derive"] }
tokio = { version = "1.52.3", features = [
    "rt-multi-thread",
    "net",
    "sync",
    "time",
] }
